services:
  nginx:
    image: nginx:alpine
    ports:
      - "3001:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - app
      - frontend
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      nysoure-net:

  app:
    build:
      context: .
      dockerfile: Dockerfile
    expose:
      - "3000"
    volumes:
      - ./app_data:/var/lib/nysoure
    depends_on:
      - postgres
      - redis
    env_file:
      - .env
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: "${APP_MEM_LIMIT:-512M}"
        reservations:
          memory: "${APP_MEM_RESERVE:-64M}"
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      nysoure-net:

  frontend:
    build:
      context: ./frontend_v2
      dockerfile: Dockerfile
    expose:
      - "3000"
    env_file:
      - frontend_v2/.env
    deploy:
      resources:
        limits:
          memory: "${FRONTEND_MEM_LIMIT:-512M}"
        reservations:
          memory: "${FRONTEND_MEM_RESERVE:-128M}"
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    restart: unless-stopped
    networks:
      nysoure-net:

  postgres:
    image: postgres:18.1
    volumes:
      - ./pg_data:/var/lib/postgresql
    environment:
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_DB=${DB_NAME}
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: "${POSTGRES_MEM_LIMIT:-1G}"
        reservations:
          memory: "${POSTGRES_MEM_RESERVE:-128M}"
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      nysoure-net:

  redis:
    image: redis:latest
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: "${REDIS_MEM_LIMIT:-512M}"
        reservations:
          memory: "${REDIS_MEM_RESERVE:-64M}"
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      nysoure-net:

  backup:
    build: https://git.nyne.dev/x/backup.git#v0.0.6
    volumes: 
      - ./backup.yaml:/config/backup.yaml
      - ./app_data:/data/app_data:ro
      - ./.env:/data/.env
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      nysoure-net:
    depends_on:
      - app
      - postgres

networks:
  nysoure-net: